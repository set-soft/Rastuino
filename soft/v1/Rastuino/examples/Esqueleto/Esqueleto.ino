#include <Rastuino.h>

void setup()
{
 Inicializar();
 //   Si hay diferencia de velocidad entre los motores podemos indicar un
 // máximo menor a 255 para el motor que necesitamos que sea más lento.
 // CalibrarMotor(1,65);
 //   Si tenemos un buzzer conectado podemos indicar en que canal.
 // SalidaBeep(4);

 // Acciones que solo se ejecutarán una vez
}

void loop() 
{
 // Acciones que se repetirán
 // Ejemplos:
 // Encender el canal 1
 // Encender(1);
 // Apagar el canal 1
 // Apagar(1);
 // Esperar 5 segundos
 // Esperar(5);
}

